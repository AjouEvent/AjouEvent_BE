version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/ajouevent-be
permissions:
  - object: /home/ubuntu/ajouevent-be/
    owner: ubuntu
    group: ubuntu
hooks:
  AfterInstall:
    - location: scripts/deploy.sh
      timeout: 60
      runas: root